services:
  flight-arc:
    build:
      context: .
      args:
        VITE_MAPBOX_TOKEN: ${VITE_MAPBOX_TOKEN}
    ports:
      - "3721:80"
    volumes:
      # 資料檔是 gitignored 的，從 host 掛載進去
      - ./public/aviation_data.json:/usr/share/nginx/html/aviation_data.json:ro
      - ./public/ship_data.json:/usr/share/nginx/html/ship_data.json:ro
      - ./public/rail:/usr/share/nginx/html/rail:ro
    restart: unless-stopped
