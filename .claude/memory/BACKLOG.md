# Backlog

> **Current index — 2026-08-19.** This is the cross-repo and production-work SSOT, not a changelog. Completed PR/test/asset-byte history belongs in git and the relevant feature changelog/handoff. A feature's detailed work has exactly one canonical feature backlog; this file retains only its release, incident, security, or cross-cutting dependency.

## Operating rules

- **Priority** orders work that is actionable now: `P0` urgent release/incident, `P1` current planning window, `P2` planned, `P3` opportunistic. A date- or upstream-gated item is not P0 merely because it matters later.
- **State** is separate from priority and is only one of: `ready`, `in_progress`, `blocked`, `waiting_external`, `conditional`, `verifying`.
- Each active item has one primary home. Do not duplicate a feature item here: use the feature index and its canonical link instead.
- `Acceptance / evidence` is the condition for closing an item; `last verified` is an observation date, not proof of production release.

## Current release gates

| ID | Priority | State | Outcome | Next action / blocker | Acceptance / canonical link |
|---|---|---|---|---|---|
| BR-2 | P1 | blocked | Make the 12 immutable Business Registry assets available in production. | Blocked on explicit production-pull/deploy authority. | Runtime asset sizes/checksums match the handoff; S3 readback alone is insufficient. [Company backlog](../../docs/features/business-registry-company-layers/backlog.md) |
| BR-3 | P1 | blocked | Establish production truth for the Business Registry experience. | After BR-2, probe all 12 assets then perform All Off browser QA. | HTTP probes, zoom split, controls, popups, legends and dark-map readability have recorded browser evidence. [Company backlog](../../docs/features/business-registry-company-layers/backlog.md) |
| Release-assets | P1 | verifying | Resolve asset/deploy gaps without duplicating per-feature implementation. | Verify each feature's PR, object readback, production HTTP and browser state; only confirmed failures become blockers. | Exact asset HTTP probe plus feature browser acceptance. [Religion](../../docs/features/religion/backlog.md), [Tourism](../../docs/features/tourism-layers/backlog.md), [Tree](../../docs/features/tree-layers/backlog.md), [Urban Heat](../../docs/features/urban-heat-lst/backlog.md), [Urban Zoning](../../docs/features/urban-zoning/backlog.md), [Vessel Watch](../../docs/features/vessel-watch/backlog.md), [Property value](../../docs/features/property-value/backlog.md), [Water resources](../../docs/features/water-resources/backlog.md), [Static-to-CDN](../../docs/features/static-to-cdn/backlog.md), [Parking](../../docs/features/parking/backlog.md), [Road congestion](../../docs/features/road-congestion/backlog.md), [Tourist shuttle](../../docs/features/tourist-shuttle/backlog.md) |
| G013 | P1 | waiting_external | Restore the corrected KHH collector on its VM runtime. | Copy the already-pushed collector to the HiCloud VM according to its runbook. | VM cron runs the corrected collector and fresh KHH rows are observed. Last verified 2026-07-26; `data-collectors/external/immigration_apis_airport_vm/README` |

## Production incidents & data health

| ID | Priority | State | Outcome | Next action / blocker | Acceptance / canonical link |
|---|---|---|---|---|---|
| PH-2 | **P1** | blocked | Decide what replaces the dengue card now that CDC removed its source. | `Weekly_Age_County_Gender_061.csv` returns **404** and the `aagstable-weekly-dengue` dataset is gone from CKAN (disappeared between 2026-08-13 and 08-20); NIDSS's own download link 404s too. Three options: scrape the NIDSS ASP.NET form (daily 08:30, already at W33, high effort) / ask CDC for the new location / retire the card and mount an A-grade candidate instead. | An owner decision plus either a working source or the card removed. Candidates: [public-health proposal](../../docs/proposal/monitor-tweaks-2026-08-21/public-health-proposal.md) |
| PR-1 | P2 | ready | Stop the inmate card from silently shrinking to empty while upstream stays dead. | Apply the prepared RPC-window-anchor migration (`max(observed_date)` instead of `now()`); written but **not applied** — needs 拍板. The file deliberately carries **no number** (`prison_population_window_anchor.PENDING.sql`): it has already been renumbered three times (367/368/369 each got taken by a parallel session). Take a number on the day it is applied, not before. Without it the 365-day window slides past the frozen 2026-05-15 data and the card empties out around 2027-05. | Card still renders its trend after the window would have passed the last data point. [SQL](../../docs/proposal/monitor-tweaks-2026-08-21/prison_population_window_anchor.PENDING.sql) |
| DS-01 | P1 | waiting_external | Stop losing 29/30 lightning snapshots once the upstream recovers. | On collector recovery notification, reset `LIGHTNING_EVENTS_INTERVAL=1`. | Consecutive one-minute source snapshots are collected; no recovery is inferred from HTTP 200 alone. [Outage pitfall](../pitfalls/2026-08-07-silent-upstream-outage.md) |
| DS-02 | P1 | waiting_external | Restore a trustworthy A1-accident feed. | Escalate the stale Police Agency source or select and document an alternative. | Freshness advances and source contract/coverage is recorded. |
| DS-03 | P2 | waiting_external | Obtain a durable response for the empty Taipower-lightning endpoint. | Report the data.gov.tw outage with reproducible evidence. | Upstream acknowledgement or an approved replacement source. |
| DS-04 | P2 | ready | Restore county-level lightning summaries. | Provide the required county polygons and rerun the summary path. | Summary has county rows rather than only `county=NULL`. |
| WF-2 | P1 | waiting_external | Avoid silently deleting ~3,700 C-level long-term-care locations. | Explain the upstream 4,232→560 change before accepting the new `medLTC` export. | Owner-approved source explanation and a documented sync/no-sync decision. [Welfare backlog](../../docs/features/welfare-layers/backlog.md) |
| BL-25 | P1 | ready | Repair transport feeds that appear healthy in metadata but fail in runtime data. | Verify/fix stale YouBike and trails materializations, flight retention dates, H3 limit, and waste city assumptions in gis-platform. | Each affected RPC is checked with recent rows and Pulse behavior. [Transport progress](../../../mini-taiwan-transport/docs/proposal/transport-lite-progress.md) |
| BL-7 | P3 | ready | Explain why reservoir daily operations stopped after 2026-04-23. | Inspect collector/cron logs before changing pipeline behavior. | New records or a documented retired/replaced source. |
| TD-1 | P2 | ready | Stop `rail_timetable` from silently dropping whole days of TRA schedules. | Two independent defects, both still live: (a) the collector schedules on a 1440-minute *relative* interval, so every Zeabur restart resets the timer and the run time drifts (measured Aug: 14:35→13:06→17:24→11:42→00:05→08:34→22:08; gaps ranged 4.2h to 44.8h) — whenever a gap spans a whole calendar day that day's `/Today` is never fetched (2026-05-31, 08-14); (b) when `convert_tra_timetable` throws, `supabase_writer.py` falls back to writing `system='tra'` instead of `'tra_daily'` and only logs a warning, so downstream sees nothing while the data sits in the DB (2026-04-02; 7 orphan rows exist). Suggested fixes: interval 1440→240 (writes are already `ON CONFLICT DO UPDATE`), and make the fallback keep `tra_daily` with a degraded flag. | 9 of 175 days lack a schedule, so to-arrival delay metrics are NULL there. **2 of the 6 are recoverable**: 2026-04-02 (919 trains, in DB as `system='tra'`) and 2026-06-26 (916 trains, in S3 `rail_timetable/archives/`). The other 4 are unrecoverable (05-04/05-05 TDX returned 0 trains across 6 retries; 05-31/08-14 the collector never ran). |

## Security & governance

| ID | Priority | State | Outcome | Next action / blocker | Acceptance / canonical link |
|---|---|---|---|---|---|
| D3 | P1 | blocked | Reduce anonymous PostgREST surface to the intended schemas. | First confirm no other gis-platform consumer relies on direct REST access to the schemas being removed. | `public`/`graphql_public` only, regression checks for shared consumers, and reversible dashboard evidence. [Launch hardening](../../docs/launch/08_POST_LAUNCH_HARDENING.md) |
| BC-4 | P1 | waiting_external | Complete production auth/CSP readiness as one work package. | Configure production OAuth URLs in Supabase/Google; then collect browser CSP violations before changing Report-Only to enforcing. | Production Google login works; all key flows have no CSP violations before enforcement. [BYOK backlog](../../docs/features/byok-chat/backlog.md) |
| OG-1 | P2 | waiting_external | Bound anon-key abuse cost. | Confirm and set Supabase Spend Cap; self-zone Cloudflare rate limits do not protect Supabase. | Dashboard setting and alert threshold are recorded. [Owner-gated backlog](../../docs/features/owner-gated-layers/backlog.md) |
| G016 | P2 | waiting_external | Revoke the obsolete `weather_change` AWS credential after completing local secret and dependency cleanup. | Local `.env` references and unused S3/boto3 code are removed; an AWS administrator must deactivate the `gis-data-collectors` key ending `E7PK`. | Old credential fails through STS and retirement is recorded without exposing secret material. |

## Cross-cutting platform work

| ID | Priority | State | Outcome | Next action / blocker | Acceptance / canonical link |
|---|---|---|---|---|---|
| AR-11e | P1 | conditional | Reclaim image bytea storage only after CDN stability is proven. | Set an owner-approved stability window; then delete 3.2GB bytea and add `pg_cron` cleanup. | CDN read path verified, cleanup job active, and deletion evidence retained; destructive action requires approval. [Imagery backlog](../../docs/features/imagery/backlog.md) |
| AR-12/13 | P1 | ready | Move C-class shared realtime snapshots from per-user DB reads to CDN delivery. | Define snapshot writer, manifest and CDN read contract. | Target consumers read immutable/current CDN snapshots with no regression. [Architecture plan](../../docs/proposal/architecture-overhaul-plan.md) |
| AR-14~16 | P1 | in_progress | Turn saved ship/bus/flight trails into CDN-consumable products. | Export exists; design processed delivery bundles instead of front-end direct reads of raw `trails/`. | Browser reads CDN products with correct day selection and controlled egress. [Architecture plan](../../docs/proposal/architecture-overhaul-plan.md) |
| AR-31~36 | P2 | ready | Improve multi-layer rendering without unrelated refactors. | Prioritize renderer merge, FlightScene, GPU time filter and worker design by measured bottleneck. | Profiling and targeted visual regression evidence meet the selected budget. [Architecture plan](../../docs/proposal/architecture-overhaul-plan.md) |
| AU-1 | P2 | ready | Guard sidebar/map/legend color consistency. | Add a contract test after confirming intended color SSOT. | Test catches divergent color definitions. [Architecture audit](../../docs/research/architecture-audit-2026-08-10.md) |
| AU-3 | P2 | ready | Eliminate divergent day-prefetch implementations. | Choose one shared path or explicitly scope the two implementations. | One documented contract with targeted tests. [Architecture audit](../../docs/research/architecture-audit-2026-08-10.md) |
| AU-4 | P2 | ready | Make Three scenes portable to MapLibre embed. | Replace handwritten projections with `toMercator()`. | Main and embed coordinate regression checks pass. [Architecture audit](../../docs/research/architecture-audit-2026-08-10.md) |
| AU-8 | P2 | ready | Give the shipped food-price monitor feature a proper documentation home. | Add the four feature documents and point this index to it. | README/backlog/changelog/handoff exist and match runtime scope. |
| CAT-1 | P2 | ready | Restore verified upstream lineage for the 9 layers now declared `catalog_missing`. | Create catalog entries in taipei-gis-analytics for the 10 dataset ids retained in each layer's manifest `note`; the four `animal_welfare.*` ids must be renamed to the all-lowercase-underscore filename convention first. | Each layer returns to `status: "verified"` with a real catalog `dataset_id`, and `upstreamRegistry`'s catalog test stays green **when run from the main tree** (worktrees skip it silently). Covers 6 telecom layers red since 2026-08-18 plus 3 animal-welfare layers. [Vessel watch backlog](../../docs/features/vessel-watch/backlog.md) |
| G004 | P1 | verifying | Shrink Docker image only if deployment asset serving remains correct. | Validate which public paths are served from `/data` before changing `.dockerignore`. | Image-size reduction plus production asset probes; agriculture/bus remain served. |
| G005 | P2 | ready | Keep waste layers visible after dark/light basemap changes. | Rebuild waste source/layer on `styledata` or revise the setup guard. | Toggle survives both style transitions. |
| G017 | P2 | ready | Provide safe, scoped Cloudflare cache purge for deploy recovery. | Add credentials and choose purge-by-prefix/by-URL; avoid `purge_everything`. | A test asset can be invalidated without flushing the 297MB embed basemap. Replaces overlapping legacy G020. |
| G022 | P2 | ready | Add ship trail LOD only if measured rendering cost warrants it. | Profile low-zoom vessel rendering; preserve port-detail quality. | Measured budget improves with a visual regression check. Renamed from legacy duplicate `G011`. |
| G023 | P2 | verifying | Decide whether Wall mode may pause hidden map engines. | Confirm product expectation before adding `setActive(false)` to engines/scenes. | Approved UX behavior and CPU comparison. Renamed from legacy duplicate `G011`. |
| G025 | P3 | ready | Make Monitor alert-series fetching incremental. | Design a recent-window RPC/cache update rather than reloading 24h×6 groups. | Network/DB work drops with equivalent displayed history. Renamed from legacy duplicate `G012`. |
| GD-1 | P2 | ready | Catch "the map silently built no sources" before a user does. | Nothing tests runtime style state — `layerConsistency` only checks manifest completeness. A 2026-08-22 regression left **161 of 264 sources uncreated** for a full day. Add a contract test asserting no geojson overlay's source spec carries an `undefined`-valued key. | The regression's exact shape (`attribution: undefined`) fails the suite. [Incident](INCIDENTS.md) 2026-08-21/22 ①; principle: style spec 禁塞 undefined |
| GD-2 | P2 | ready | Catch mojibake before it is written to the DB. | Nothing checks that Chinese text written by collectors is actually Chinese; NCDR alerts wrote garbled rows intermittently from 2022 to 2026 unnoticed. Add a write-time guard rejecting Cyrillic (`[Ѐ-ӿ]`) in text columns — Taiwanese disaster alerts can never legitimately contain it — and alert instead of storing. | A deliberately mis-decoded payload is rejected and raises. Lives in `gis-data-collectors`. [Incident](INCIDENTS.md) 2026-08-21/22 ② |
| FP-1 | P2 | ready | Move the food-price **index** pipeline off the local Mac onto the same 24/7 footing as every other collector. | Blocked on history: the light thresholds use a rolling **1095-day median**, but `live.food_price_daily` holds only ~24 days (the 3.64M-row raw backfill was never loaded into Supabase — migration 334 flags the retention/partition work that implies). Either load the raw history first, or have the collector read local parquet (which defeats the decoupling). | Index lands daily without a human or a Mac being awake. **Interim (done 2026-08-21): option A** — launchd `com.gis.foodprice_index` runs `pipelines/food_prices/wholesale_prices/run_daily.sh` at 06:00; it does not run while the Mac sleeps. Root cause and options: [food-price stale recovery](../../docs/proposal/monitor-tweaks-2026-08-21/food-price-stale-recovery.md) |
| FP-2 | P2 | ready | Detect a stalled food-price index within days instead of weeks. | `analytics.food_price_index_daily` has no timestamptz column, so `check_collector_freshness` cannot cover it; `realtime_tables.yaml` only watches the raw table, which stays green forever. Add a `loaded_at` column (new migration) or a `max(trade_date)`-based check. Threshold must allow 3–4 days (T+1 + weekend + holiday market closures). | A stall raises an alert. `realtime_tables.yaml` is a shared registry — wire it by hand, not from an agent. Same source doc as FP-1. |
| with-conn-timeout | P2 | ready | Bound custom SQL and long collector tasks. | Add transaction-local timeout coverage for `satellite_passes` and `waste_match`. | Long calls terminate predictably without affecting unrelated pooled requests. |
| Unrouted-platform-programs | P2 | ready | Keep active programs without a feature backlog discoverable. | Split them into feature backlogs when actively scheduled; until then use the named plan as canonical. | Water `BL-4/6/8–23`, climate/terrain `MC-*`, monitor `MO-4/7/8/12–15`, satellite `SAT-*`, and road coverage `CV-*` each retain a canonical plan. `docs/energy-v2-plan.md`, `docs/proposal/monitor-mode.md`, `docs/proposal/satellite-console.md`, `docs/bus-layer-design.md` |
| TD-4 | P3 | conditional | Keep the TRA delay trajectory column from growing without bound. | `analytics.tra_train_delay_daily.delay_trajectory` is 72% of the table (138MB across 175 days ≈ 288MB/year). Raw is fully recoverable from S3 (`tra_train/archives/`, continuous since 2026-02-28), so trimming is safe. No action needed yet — revisit if Supabase storage becomes a constraint. | A retention rule that keeps numeric metrics indefinitely and trajectories only for recent months. |

## Decision queue

| ID | Priority | State | Outcome | Next action / blocker | Acceptance / canonical link |
|---|---|---|---|---|---|
| BR-5 | P2 | blocked | Add science-park analysis only from authoritative geometry. | Acquire provenance-backed polygons; do not infer geometry from membership. | Explicit flags, source, tests and handoff. [Company backlog](../../docs/features/business-registry-company-layers/backlog.md) |
| BR-6 | P2 | blocked | Improve Business Registry geocoding without substituting company coordinates. | Select an authoritative address/geocode source and preserve precision/provenance. | Misses decrease with denominator stable; unresolved reasons retained. [Company backlog](../../docs/features/business-registry-company-layers/backlog.md) |
| WF-4 | P2 | blocked | Make long-term-care coverage analysis semantically valid. | Owner chooses registered institutions or contracted units; never UNION them. | Chosen denominator/method is stated before implementation. [Welfare backlog](../../docs/features/welfare-layers/backlog.md) |
| MO-6 | P3 | ready | Add a central-bank exchange-rate signal; TWSE work is no longer part of this item. | Define source, freshness and card/timeline use. | Rate data and provenance appear in Monitor. Merges legacy MO-6/MO-16. `docs/proposal/monitor-mode.md` |
| FE-01 | P2 | ready | Make share URLs truthful about parameter support. | Either apply `p.*` slider state in App or remove dead parsing. | Shared link reproduces state or no longer claims to. |
| G003 | P3 | blocked | Decide whether the independent Three showcase remains tracked. | Owner decides retain versus move to examples. | Decision and path are documented; app build is unaffected. |
| G015 | P3 | blocked | Decide whether to revive draggable Monitor layout work. | Compare retained commits with the current monitor packing design. | A keep/rebase/close decision with evidence. |
| G018 | P2 | waiting_external | Feed rail turnback geometry artifacts to upstream data owner. | Send reproducible examples and decide whether remediation belongs upstream. | Upstream acknowledgement or documented local tolerance. |
| G019 | P3 | verifying | Determine whether 83 unused rail tracks are data defects or deploy waste. | Compare schedules, missing service and track inventory before deleting anything. | Evidence-backed keep/delete decision. |
| G021 | P3 | ready | Align gis-platform's gis-wiki submodule. | Verify intended upstream SHA and update in its owning repository. | Superproject points to approved gis-wiki main commit. |
| TD-2 | P3 | conditional | Decide whether pre-2026-03 TRA delay history is worth backfilling. | 64 legacy-format days (2025-12-26~2026-02-27) remain in S3 but were **explicitly declined** on 2026-08-22. They predate `reference.daily_schedules` (starts 2026-03-03), so they would carry observation-side metrics only — no `scheduled_trains`, `coverage_pct` or to-arrival delay. Reopen only if a longer baseline is actually needed. | `scripts/backfill_tra_delay_from_s3.py --from 2025-12-26 --to 2026-02-27`; slower legacy per-file format. |
| TD-3 | P2 | ready | Replace the provisional `traDelay` grid coordinates with a sandbox export. | Coordinates (`x0,y63,w5,h5` plus `vesselZone` h 11→6) were hand-picked at wiring time, not dragged. They are the only position that keeps the top level as "three columns + bottom two columns (5+7)"; `monitorPacking` asserts this. Both wide and split layouts need re-dragging. | `MONITOR_LAYOUT` and `MONITOR_LAYOUT_SPLIT` regenerated from the sandbox rather than hand-edited. `docs/features/monitor-grid-static/sandbox.html` |

## Scheduled/conditional

| ID | Priority | State | Outcome | Next action / blocker | Acceptance / canonical link |
|---|---|---|---|---|---|
| BR-7 | P1 | conditional | Preserve the 202609 company-stock snapshot before it becomes unavailable. | **not_before=2026-09-05**: run the existing `company_stock` pipeline. | Raw/processed/QA/manifest/checksum retained; row count and coordinate reconciliation pass. [Company backlog](../../docs/features/business-registry-company-layers/backlog.md) |
| MC-5 | P3 | conditional | Refresh static LST after the 2026 warm season. | **not_before=2026-10-01**: rerun median pipeline and review color ranges. | Replaced asset has date/provenance and expected P2–P98 ranges. [Urban Heat backlog](../../docs/features/urban-heat-lst/backlog.md) |
| EM-30 | P2 | conditional | Remove rail-geometry fallback only after an observation window. | Set a final observation date, then remove code and old S3 object together with approval. | Production has used the hashed asset through the window and no fallback fetch occurred. [Embeddable map backlog](../../docs/features/embeddable-map/backlog.md) |

## Verification/cleanup queue

| ID | Priority | State | Outcome | Next action / blocker | Acceptance / canonical link |
|---|---|---|---|---|---|
| G009 | P2 | verifying | Resolve conflict with AR-03's claim that loadingRegistry gaps were fixed. | Re-run a focused inventory; create a new scoped task only for proven residual gaps. | Evidence either closes G009 as superseded or creates a unique residual ID. |
| PL-1 | P2 | verifying | Resolve conflict between the old PLA layer plan and the deployed PLA feature. | Compare the proposal with current feature backlog/runtime before retaining any work. | Supersede or rewrite as a distinct unmet requirement. [PLA backlog](../../docs/features/pla-activity/backlog.md) |
| PA-1 | P2 | verifying | Make one canonical decision for the historical PLA track backfill. | Confirm whether legacy DS-05 is fully the same scope, then keep only PA-1. | One active record with dataset/date scope and reproducible completion evidence. [PLA backlog](../../docs/features/pla-activity/backlog.md) |
| GC-7 | P2 | verifying | Separate completed CMEMS bbox expansion from any remaining CAMS work. | Inspect current collector configuration and split residual work into a unique item if needed. | CMEMS/CAMS scope is independently evidenced; no combined misleading status. [Global Climate backlog](../../docs/features/global-climate/backlog.md) |
| G008 | P3 | verifying | Reassess residual large-file modularization after manifest migration. | Measure remaining files and identify a user-facing or maintenance pain point. | Close as no-action or create narrowly scoped follow-ups; do not cite retired `useTransportParams` as blocker. |
| AR-41~44 | P2 | verifying | Rewrite the obsolete schema→auth→chat sequence. | Compare AR-21 completion, D3 and BC state; split only still-unmet dependencies. | A non-overlapping successor set or a justified close. |
| G024 | P3 | verifying | Close/archive legacy tourism-canopy deployment record after checking its claimed production evidence. | Confirm asset probe and feature handoff; it is not an active G012. | Close with evidence or create a new release-gate ID. Renamed from legacy duplicate `G012`. |
| Legacy-ID-migration | P3 | verifying | Keep historic links intelligible while restoring global ID uniqueness. | Update future references: `G011(ship LOD)→G022`; `G011(wall pause)→G023`; `G012(tourism/canopy)→G024`; `G012(alertSeries)→G025`; `G020→G017`; mountain-safety `MS-1~3→MTS-1~3`; `DS-05→PA-1` after scope verification. | Targeted search has no ambiguous active IDs; old IDs remain only as migration notes. |
| G026 | P2 | ready | Drop the production backup tables and the rewrite backup branch once their sources are confirmed good. | Deliberately retained pending owner sign-off: `live.public_health_weekly_backup_20260821` (174,002 rows), `live.disaster_alerts_mojibake_backup_20260822` (95 rows), and branch `backup/pre-lfs-rewrite-20260822` in **mini-taiwan-info** (holds the pre-filter-branch history of 11 commits, including the 218MB geojson that was stripped). | Each source verified correct, then dropped/deleted in one scoped action. |
| G027 | P3 | ready | Clear the remaining workspace residue left untouched on 2026-08-22. | All deferred by owner decision, none blocking: 3 worktrees kept for unharvested work (`mini-taiwan-pulse/chore/ar11e-legacy-rpc-retire` 3 commits — blocked on R2 CORS; `satellite-art/perf/webgpu-sgp4` 1 commit, 946 lines; `terrain-art/feat/typhoon-vortex` — only `node_modules`, safe to remove anytime); uncommitted source edits in 5 repos (`mini-taipei` 15, `uk-trip` 9, `train_map` 4, `open-air-gis` 4, `mini-taipei-v2` 4); `youbike_analytics_system` notebook (+12,877 lines, execution output); `taiwan-md` 3 staged files blocked by a husky hook requiring `bunx` (not installed); `.opendata-memory` committed locally but has no remote. | Each item either resolved or explicitly closed as won't-do. |

## Weekly audit findings

> Appended by the `weekly-audit` skill. One `WA-*` ID per finding that needs cross-week tracking;
> transient or already-fixed findings stay in the weekly report only.
> Report: [2026-W34](../../docs/audit/weekly/2026-W34.md)

| ID | Priority | State | Outcome | Next action / blocker | Acceptance / canonical link |
|---|---|---|---|---|---|
| WA-1 | P1 | ready | Remove the dangling `fallbackUrl` on the two owner-gated agriculture layers. | `livestockFarms` / `slaughterhouses` still declare `./agriculture/*.geojson` fallbacks, but those files are deliberately excluded from the CDN by `scripts/deploy/pull-deploy-assets.sh:59-64`; an RPC permission failure falls back to a 404. | Both layers either drop `fallbackUrl` or point at `_empty.geojson`; `probe_layers` reports 0 unexpected 404s. |
| WA-2 | P1 | ready | Explain why `live.traffic_accidents_a1` has never been written since its collector went live. | Collector enabled 2026-07-03; latest row is 2026-06-27 — earlier than the enable date, so "no new incidents" does not explain it. Check the NPA upstream feed and whether the collector has ever succeeded. | Either fresh rows appear, or the upstream break is documented and the table is marked event-driven-idle with evidence. |
| WA-3 | P2 | ready | Give the large `live` time-series tables an explicit retention policy. | 63 tables over 100MB have no cleanup/prune job naming them; `live` is 33GB = 89% of the database. Audit which are genuinely uncovered vs handled by a generic partition cleaner. | Every >100MB `live` table is either covered by a cleanup job or explicitly recorded as keep-forever with a reason. |
| WA-4 | P3 | ready | Retire the orphan `public` RPCs. | 86 candidates after excluding extension-owned, cron-referenced and frontend-called functions. Residual false positives: RLS policy internals and SECURITY DEFINER call chains. | Batches of ~20 reviewed per week; each function is dropped or documented as internally called. |
| WA-5 | P2 | ready | Decide what to do with 41 orphan assets (809 MB on disk, unreferenced by the manifest). | Largest is `base_map/taiwan_basemap.pmtiles` (283 MB). Confirm each is genuinely unused before deleting anything. | Each asset is either referenced, deleted, or recorded as an intentional standby copy. |
| WA-6 | P3 | blocked | Archive the stale top-level docs. | 9 files are unlinked from any index and older than 60 days (oldest `NEWS_MAP_PLAN.md`, 167 days). Moving docs is outside the audit auto-fix whitelist. | Needs an explicit decision on where they go; not to be moved automatically. |
| WA-7 | P2 | ready | Push or retire the two long-idle local branches. | `feat/monitor-widgets-batch1` (44 days) and `feat/monitor-grid-layout` (43 days) have no upstream; single-machine loss risk. 17 unpushed branches in total. | Both branches are pushed or deleted with a recorded reason. |
| WA-8 | P3 | ready | Reassess the 19 tracked files over 5MB. | Largest is `public/forestry/forest_reserve.geojson` (44.6 MB). Other large assets already moved to S3; these did not. | Each file is either moved to S3 + gitignored, or recorded as intentionally version-controlled. |
| WA-9 | P3 | ready | Find out why two sampled assets return `BYPASS` instead of `HIT`. | `geo/cctv.geojson` and `welfare/nursing_homes_national.geojson`; the other 6 of 8 samples are `HIT`. | Both return `HIT`, or the bypass is explained by an intentional cache rule. |
| WA-10 | P2 | waiting_external | Produce the first trustworthy weekly slow-query ranking. | `pg_stat_statements` was reset on 2026-08-21 during audit development, so this week's numbers cover a partial window only. Next week's run diffs against this week's raw snapshot. | A ranking derived from snapshot deltas, each entry annotated with its caller (frontend / cron / upstream repo). |
| WA-11 | P2 | ready | Remove the 31 duplicated objects sitting in both `deploy-assets/` root and their category sub-prefix. | Same basename and identical byte size in two paths (e.g. `ftw_fields_2025.pmtiles` in root and `agriculture/`); roughly 365.6 MB, ~10% of the bucket. Likely leftovers from the flat-to-nested migration. | Every duplicate is confirmed unreferenced by any deploy script or nginx location before deletion; `collect_storage` reports 0 duplicate groups. |
| WA-12 | P3 | ready | Give `climate/frames/` a retention policy. | 322 timestamped frames, 220.9 MB, spanning 2026-07-06 to 08-25 including future forecast timestamps, with no visible cleanup. | Either an S3 lifecycle rule or an upstream collector retention window is in place and documented. |
| WA-13 | P3 | blocked | Make R2 usage observable. | Bucket is `mini-tw-pulse` (documented in `taipei-gis-analytics/docs/handoff/read-path-cdn-imagery.md`), but the token lacks ListObjectsV2 and `rclone size` returns 403. | Needs the permission added in the Cloudflare dashboard; then `collect_storage` reports R2 bytes and cost estimate. |

## Feature backlog index

Feature detail, UX matrices, popup fields, implementation subtasks and feature-level verification live in the linked backlog. This index prevents them from being copied into the core queue. `_TEMPLATE` is intentionally excluded.

| Program | Canonical backlog | Cross-cutting reference in this file |
|---|---|---|
| Agriculture / aquaculture / livestock | [Agriculture](../../docs/features/agriculture/backlog.md), [Aquaculture](../../docs/features/aquaculture/backlog.md), [Livestock](../../docs/features/livestock/backlog.md) | — |
| Business Registry | [Company layers](../../docs/features/business-registry-company-layers/backlog.md), [Industrial layers](../../docs/features/business-registry-industrial-layers/backlog.md), [common addresses](../../docs/features/common-registration-addresses/backlog.md) | BR-2/3/5–7 |
| Civic, culture, education, tourism, religion, tree | [Civic](../../docs/features/civic-facilities-layers/backlog.md), [Culture](../../docs/features/culture-layers/backlog.md), [Education](../../docs/features/education-layers/backlog.md), [Tourism](../../docs/features/tourism-layers/backlog.md), [Religion](../../docs/features/religion/backlog.md), [Tree](../../docs/features/tree-layers/backlog.md) | Release-assets, G024 |
| Climate, imagery, terrain, urban form/heat/zoning | [Global climate](../../docs/features/global-climate/backlog.md), [Imagery](../../docs/features/imagery/backlog.md), [Terrain](../../docs/features/terrain-vector/backlog.md), [Buildings night lights](../../docs/features/buildings-night-lights/backlog.md), [Urban form](../../docs/features/urban-form/backlog.md), [Urban heat](../../docs/features/urban-heat-lst/backlog.md), [Urban zoning](../../docs/features/urban-zoning/backlog.md) | AR-11e, GC-7, MC-5, Release-assets |
| Emergency, fire, mountain safety, welfare | [Earthquake replay](../../docs/features/earthquake-replay/backlog.md), [ER hospital](../../docs/features/er-hospital/backlog.md), [Fire rescue](../../docs/features/fire-rescue/backlog.md), [Mountain safety](../../docs/features/mountain-safety/backlog.md), [Welfare](../../docs/features/welfare-layers/backlog.md) | WF-2/4, MTS-1~3, Release-assets |
| Embed, map platform, layer manifest, bloom | [Embeddable map](../../docs/features/embeddable-map/backlog.md), [Layer manifest](../../docs/features/layer-manifest/backlog.md), [Bloom](../../docs/features/bloom-experiments/backlog.md), [Chart tooltip](../../docs/features/chart-hover-tooltip/backlog.md) | EM-30, AU-1–4 |
| Monitor, news, owner gating, BYOK | [Monitor grid](../../docs/features/monitor-grid-static/backlog.md), [Monitor split](../../docs/features/monitor-split/backlog.md), [News](../../docs/features/news/backlog.md), [Owner-gated](../../docs/features/owner-gated-layers/backlog.md), [BYOK](../../docs/features/byok-chat/backlog.md) | BC-4, OG-1, MO-6, G023/G025 |
| Mobility and place | [Bus](../../docs/features/bus/backlog.md), [Parking](../../docs/features/parking/backlog.md), [Road congestion](../../docs/features/road-congestion/backlog.md), [Tourist shuttle](../../docs/features/tourist-shuttle/backlog.md), [Vessel watch](../../docs/features/vessel-watch/backlog.md), [Real estate](../../docs/features/real-estate/backlog.md), [Street trees](../../docs/features/street_trees_taipei_diff/backlog.md) | G022, Release-assets |
| PLA activity | [PLA activity](../../docs/features/pla-activity/backlog.md) | PA-1, PL-1 |
| Funeral | [Funeral layers](../../docs/features/funeral-layers/backlog.md) | — |
| Property value / static delivery / water | [Property value](../../docs/features/property-value/backlog.md), [Static-to-CDN](../../docs/features/static-to-cdn/backlog.md), [Water resources](../../docs/features/water-resources/backlog.md) | Release-assets, AR-12/13 |

Older completed work is intentionally discoverable through git log, feature changelogs/handoffs and release evidence—not this active index.
