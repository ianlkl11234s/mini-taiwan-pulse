import { describe, expect, it } from "vitest";
import { parseJevRunReceipt, receiptLayersForCandidates, type JevRunReceipt } from "./engine";
const layer = (key: string, relevant: boolean | null) => ({ key, label: key, theme: null, group: null, sourceKinds: ["custom"], sourceStatus: "verified", sourceTime: "unknown (not evaluated by screening)", license: "unknown (not evaluated by screening)", coverage: "unknown (not evaluated by screening)", geometryPrecision: "unknown (not evaluated by screening)", batchId: "batch-001", decision: { status: relevant === null ? "unknown" as const : "decided" as const, relevant, probability: relevant === null ? null : 0.7, answerType: relevant === null ? null : "noul" as const, confidence: null } });
const receipt: JevRunReceipt = { version: "jev-layer-screening-receipt/v1", mode: "live", runId: "run-1", query: "教育資源", provider: "openrouter", model: "typesafe/jev-1.13", startedAt: "2026-09-22T01:00:00Z", endedAt: "2026-09-22T01:00:01Z", wallDurationMs: 1000, relevanceThreshold: 0.5, manifestFingerprint: "manifest-sha256", indexFingerprint: "index-sha256", candidateCount: 2, lockedExcludedCount: 1, batches: [{ batchId: "batch-001", requestId: "req-1", requestBytes: 10, candidateCount: 2, durationMs: 1000, status: "completed", error: null, startedAt: "2026-09-22T01:00:00Z", endedAt: "2026-09-22T01:00:01Z" }], layers: [layer("school", true), layer("port", false)], plannedWebsiteActions: [{ action: "search_layers", executed: false, note: "planned" }], websiteReady: false };
describe("Jev runner receipt adapter", () => { it("accepts the runner v1 schema", () => expect(parseJevRunReceipt(receipt)).toEqual(receipt)); it("keeps recorded education and non-relevant decisions", () => expect(receiptLayersForCandidates(receipt, [{ key: "school" }, { key: "port" }]).map((item) => item.decision.relevant)).toEqual([true, false])); it("only emits supplied candidates", () => expect(receiptLayersForCandidates(receipt, [{ key: "port" }]).map((item) => item.key)).toEqual(["port"])); });
