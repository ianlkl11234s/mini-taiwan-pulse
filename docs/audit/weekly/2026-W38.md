# Weekly Audit 2026-W38（2026-09-14 ~ 09-20）

> 執行：2026-09-20 · 主 checkout 收集器 6/8 成功；cleanup worktree 完成文件收集器修復與重跑
> 證據邊界：本報告分開標示 repository、local runtime、production HTTP、DB、S3/R2；沒有把其中一種當成另一種的驗收。

## 摘要

**🔴 0 · 🟡 需持續處理 · 🟢 README／規範／巡檢守門已修**

專案程式基線健康：cleanup branch 以最新 `origin/master` 建立，`npx tsc -b` 與完整 Vitest
通過（234 files passed / 1 skipped，1,751 tests passed / 10 skipped）。README 的 manifest、production themes、資料類型、
loader、scene、dataset 與 nginx 數字已由巡檢腳本逐項對帳；正式 UI 是 62 themes / 569 toggles，
registry 則是 778 keys，兩者不再混稱。

本週主要風險不是主程式測試，而是 Git 保存面與長期 metadata：cleanup worktree 建立後共有
89 個 registered worktrees；其中 12 個已驗證為乾淨且遠端可追溯，移入 macOS 垃圾桶的專用資料夾，
失效 metadata 已在建立本機復原包後 prune，現在只剩 18 個實際存在的註冊 worktree。16 個 local
branches 對應 13 個 unique heads 未被任何 fetched `origin/*` 包含；安全盤點後一律只留本機，不推
archive refs。主 checkout 的既有程式／文件已保存為 local-only commit `57bd08ea` 並恢復 clean；
本輪沒有刪 branch 或覆蓋工作樹。

## 趨勢

| 指標 | 本週 | 2026-W34 | 說明 |
|---|---:|---:|---|
| Supabase DB | 39 GB | 37 GB | 本輪 DB snapshot |
| `live` schema | 657 tables / 32 GB | 509 / 33 GB | table 數增加；容量未異常跳升 |
| pg_cron | 90 jobs，近 7 天成功 | 71 jobs，全成功 | DB evidence |
| S3 deploy assets | 9.96 GB / 8,947 objects | 3.74 GB / 616 | 含大量新分類資產 |
| S3 月費牌價估算 | US$0.25 | US$0.09 | 非帳單，不含 request/egress |
| 正式首頁 | HTTP 200 / 0.738 s | 200 / 1.12 s | 只證明 HTTP，不等於各 layer render |
| CDN 抽樣 | 4/8 HIT | 6/8 HIT | 其餘需追 cache policy |
| 本地 branches | 154 | — | 41 未 push；13 unique heads 不在任何 fetched origin ref |
| registered worktrees | 18 | — | 71 筆失效 metadata 已備份後 prune；12 個 clean worktree 位於專用垃圾桶 |
| `public/` | 4.02 GB / 1,169 files | 3.37 GB / 1,125 | clean worktree 缺 ignored assets，容量以主 checkout 為準 |

## 🟢 本輪已處理

1. **README 重新對帳**：778 manifest keys、62 production themes、569 toggles、A/B/C/D
   129/98/52/499、326 upstream datasets、83 loaders、19 scenes、66 nginx locations、74 feature folders。
2. **文件 SSOT 收斂**：`docs/development-rules.md` 改為 manifest-first；移除手改 color/icon/upstream
   的舊流程；`public/` 規則改為 domain folder + URL contract，不做破壞性搬檔。
3. **AR-22 文件重寫**：`docs/features/layer-manifest/README.md` 只保留現行契約，歷史 phase
   導向 changelog/backlog/handoff，避免舊 key/test 數混入 onboarding。
4. **feature 文件生命週期**：`docs/features/README.md` 改為最小文件契約，不再要求建立空白四件套。
5. **weekly audit 可在 linked worktree 執行**：修正 JSON import、`import.meta.env`、production
   `THEMES` 口徑與 sibling repo 路徑；文件巡檢重跑為 0 red、README 對帳全綠。
6. **動態 JP height source 語意**：runtime probe 不再把 viewport-managed template ID 當作必須常駐的
   Mapbox source，並補契約測試與 skill trap，避免 false positive。
7. **sidebar accessibility**：桌機與手機的 theme/layer/location 互動補鍵盤啟動、ARIA expanded/pressed
   與可聚焦語意；focused tests 與 TypeScript 通過。
8. **正式站 smoke**：production 首頁、sidebar、搜尋「日本」、啟用 `satellitesJapan` 與 legend 載入成功，
   browser console 無 error。這只驗一條代表路徑，不代表 569 個 toggle 已逐層 render 驗收。
9. **layer 搜尋 metadata**：索引新增 upstream dataset IDs、source kinds/source IDs，結果顯示 manifest
   的可追溯摘要；owner-gated 與 orphan layer 維持 fail-closed，不把自由文字實作細節回傳給使用者/MCP。
10. **可逆 worktree 清理**：12 個 clean、remote-reachable 且無獨有 tracked/untracked 資料的 worktree
    已移至 `~/.Trash/mini-taiwan-pulse-cleanup-20260920/`；71 筆失效 metadata 已在保存 8.1 MB 復原包後
    prune。復原包為主 checkout 的 `recovery/git-worktree-metadata-20260920.tar.gz`，SHA-256
    `4a9fd9bb4b8d728cc78f850b71ae1b0fa3dab3415b65384f4b1aaaefeda4970c`。連同 1.0 GB 可重建 build
    cache，專用垃圾桶現約 10 GB，尚未真正釋放容量。
11. **popup／UI 候選重查**：舊報告的 29 個 no-popup 候選中，25 個已有 FeatureInfo、4 個刻意使用
    tooltip；沒有可直接批次修補的漏接。basemap label toggle 完成等價去重，廣泛 sidebar 重構延後。
12. **cleanup branch 已送審**：commit `433d8a52` 已推至 `codex/project-cleanup-20260920`，PR #324
    已建立；本報告後續狀態修正另以文件 commit 補上。沒有 merge 或部署。

## 🟡 待處理／需拍板

### Git 與工作樹

- 16 個 local branches 對應 13 個 unique heads 未被任何 fetched `origin/*` 包含。多數是落後數百個
  commits 的完整舊 snapshot，包含大型 public assets；最新的 `codex/history-restore-drill-backlog-20260920`
  雖只 ahead 2，tree 仍有 private-access／evidence 痕跡。本輪決定一律只保留本機 branch refs，不推遠端。
- 主 checkout 的既有程式／文件已保存為 local-only commit `57bd08ea`，沒有推遠端；11 份日本 raw
  GeoJSON、兩份 map-story artifacts 與原始 architecture audit 已移到 GIS 父目錄的
  `_local-archive/mini-taiwan-pulse-20260920/`。Japan refinement 的 tracked tree 已 clean，其他本機 raw
  source 依使用者決定保留，不上傳遠端。
- 主 checkout 原有 31 個 tracked diffs 已完整納入 preservation commit；這是復原快照，不代表要把舊版
  工商／JP tourism 實作重新整合進 master。
- Japan refinement checkout 的 4 個未追蹤 PMTiles 已核對 immutable artifact SHA 與 production readback，
  本輪移至 macOS 垃圾桶專用資料夾；該 worktree 現已 clean，ignored source GeoJSON 保留。
- 56 個原 missing-worktree heads 已可由某個 `origin/*` reach，但「在遠端 feature branch」不等於
  「已 merge master」；本輪只 prune 失效 worktree metadata，沒有刪除任何 branch。

### Layer／資料契約

- manifest 搜尋已可按 dataset ID、source kind/source ID 找 layer，但仍不足以按 license、coverage、
  freshness、geometry、missingness 或 query capability 找 dataset。未來 MCP 應拆成
  LayerDescriptor → DatasetDescriptor → AccessDescriptor → Receipt；接手範圍見
  [`layer-discovery-mcp-handoff-20260920.md`](../../features/agent-research-workbench/layer-discovery-mcp-handoff-20260920.md)。
- `DATASET_WHITELIST` 與 manifest 分立，且 `custom` source 的結構化 metadata 不完整；先補 descriptor
  與 pagination/bbox/field projection，再開放 MCP 查詢，避免把整份 GeoJSON 放入 context。
- 舊有 29 個 no-popup 候選已排除；另有房地產 Grid ×3、`temperatureWave`、`waterFloodExtreme`、
  `powerPoles` 共 6 個 EDGE 候選，需逐層 browser 與 source semantics 決策。
- BusScene opacity 尚有 TODO；basemap label control 已完成同檔等價去重。desktop/mobile sidebar 的廣泛
  共用化會改變互動契約，留到後續有測試的工作切片。

### 文件、儲存與營運

- 11 個 feature folders 缺現行 README／交接件，22 個超過 60 天未實質更新；本輪不建立空骨架假裝完成。
- 10 份頂層 docs 是無反向連結的 archive candidates；需先逐份確認現行引用，再搬 `docs/archive/`。
- S3 有 198 組同名同大小候選（約 1.03 GB）；這只是候選，不證明內容相同或無 runtime 引用。
- climate frames 591 files / 398.2 MB 尚無 retention；R2 list 403，因此本輪無法宣稱 R2 用量正常。
- 105 個 orphan RPC candidates 與 40 個 slow query snapshots 都需要逐項排除假陽性，禁止批次刪除。
- 主 checkout 的 `.DS_Store`、約 485 MiB build 與 549 MiB OSRM build 已移專用垃圾桶；OSRM receipts、
  result、原始 PBF 與 54 MiB persistent statistics cache 保留。其餘 nested worktrees 與 recovery
  snapshots 仍含私有 env、原始資料或交接／復原價值，不列入自動刪除。

## 盲區

- 本輪沒有完成全 layer 的實際 browser render/click/popup sweep；HTTP 200、單測與 source inventory
  不等於使用者畫面正常。
- cleanup worktree 沒有本機 Mapbox/Supabase build-time env，因此 local browser 會停在黑畫面；本輪沒有讀取
  或複製主 checkout 的 secret。UI 修改以 focused tests／tsc 驗證，production smoke 驗的是部署中舊版基線。
- ignored/private assets 不會出現在 clean worktree；資產數量與容量只採主 checkout 的唯讀盤點。
- storage duplicate、orphan RPC、stale docs 與 6 個 EDGE popup 都是 candidates，未經引用與語意核對不刪除。

## 下一道 gate

1. 等待 PR #324 CI 與 review；合入時使用普通 merge commit，不 squash/rebase。
2. PR 合入後，再從乾淨 checkout 將 local `master` fast-forward 到遠端最新 commit/tag。
3. 使用者之後自行清空本次專用垃圾桶資料夾，以釋放約 10 GB。
