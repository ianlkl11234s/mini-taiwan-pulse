VITE_MAPBOX_TOKEN=your_mapbox_access_token_here
VITE_WASTE_MATCHED_TRAILS=1
FR24_API_TOKEN=your_flightradar24_api_token_here

# CWA 衛星/雷達影像的 frame bytes 來源（⚠️ AR-11e 起為必填，不再有 DB fallback）
# metadata 走 get_cwa_imagery_manifest RPC，影像本體 = `${VITE_IMAGERY_CDN_BASE}/${image_key}`。
# 未設 → 雲圖 / 雷達圖層不會有畫面（console 會印出設定指引）；舊的 bytea 讀取 RPC 已下架。
# production 用綁了 custom domain 的 R2（CF edge cache）：
VITE_IMAGERY_CDN_BASE=https://data.itsmigu.com
# 🔴 已知問題（2026-08-13）：R2 bucket 尚未設 CORS 政策，瀏覽器抓圖會被擋
#    （console: "blocked by CORS policy: No 'Access-Control-Allow-Origin' header"）。
#    線上與本機皆然，非本機設定問題。修法見 gis-platform/migrations/
#    344_ar11e_drop_legacy_imagery_rpcs.sql 檔頭的 blocker 段。
# 備援：r2.dev 公開 base（有 rate limit，只建議臨時開發用）
# VITE_IMAGERY_CDN_BASE=https://pub-eae6980c040441adbd1eaded2870d3d2.r2.dev

# S3 (upload script)
S3_BUCKET=migu-gis-data-collector
S3_ACCESS_KEY=your_aws_access_key_here
S3_SECRET_KEY=your_aws_secret_key_here
S3_REGION=ap-southeast-2

# ── EM-06 嵌入版 /embed 底圖 ────────────────────────────────────────────
# Protomaps OSM 底圖（PMTiles）位置。不設則用預設 /base_map/taiwan_basemap.pmtiles，
# 走既有部署管線（public/base_map/ → S3 deploy-assets/base_map/ → 容器 /data/base_map/）。
# 若改放 Cloudflare R2（egress 免費、適合高流量嵌入）在此填完整 URL：
# VITE_EMBED_BASEMAP_URL=https://<r2-custom-domain>/base_map/taiwan_basemap.pmtiles
